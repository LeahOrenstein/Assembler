#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/*Helper functions*/

void trimWhitespace(char* str);

char* AllocateMemFileName(char* fileName, size_t size);


#endif